import { defineConfig } from 'astro/config';

// Set `site` to your production URL once you've picked a host/domain
// (needed for canonical links / sitemaps, not for the build itself).
export default defineConfig({});
